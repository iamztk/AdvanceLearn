索引：

	InnoDB和MyISAM索引方面的区别：
		InnoDB：主键索引和辅助索引是不同的。
			主键索引：索引和数据存放在一起
			非主键索引：索引和数据是分开存放的，非主键索引中存放的是索引+主键值，然后再进行主键索引的查询，这个再次进行主键索引的查询叫回表。	
				但是也不是每次都会回表查询，如果查询的数据在非主键索引的节点可以将数据查出，就不会进行回表操作了，而是直接返回对应的值。
		
		MyISAM：主键索引和辅助索引没有区别，索引和数据是分开的
			索引中存放的是索引值和数据指针
			